Movie Recommendation System

The recommendation system was built in python using KNN or K-nearest neighbours which is a machine learning algorithm that aims to find clusters of similar users
based on common item ratings, and make predictions using the average rating of top-k nearest neighbors.

